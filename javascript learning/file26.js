// objects are refrence type
// arrays are good but not sufficients
//for real world data
// objects store key value pairs
// objects dont have index

// how to create objects

const person ={
    name:"naveed",
    age:20,
    hobbies:["guitar" ,"singing","football"]
    // we can create array in objects 

}
console.log(person);

//how to access data frim objects
console.log(person.name);
console.log(person.age);

//how to add key value pairs to objects

person.gender ="male";
console.log(person);